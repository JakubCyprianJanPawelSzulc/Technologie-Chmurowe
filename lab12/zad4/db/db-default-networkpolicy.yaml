apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: db-default
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/db-default: "true"
  podSelector:
    matchLabels:
      io.kompose.network/db-default: "true"
